<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "CS15 Bookstore" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <!-- Application styles & scripts -->
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <!-- Optionally add a Google font for a modern look -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  </head>

  <body class="bg-gray-50 text-gray-800 min-h-screen font-sans" style="font-family: 'Inter', sans-serif;">
    <!-- Main container with padding -->
    <div class="min-h-screen flex flex-col">
      <header class="fixed top-0 left-0 w-full bg-red-400 shadow-sm z-50">
        <div class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
          <h1 class="text-xl font-semibold">
            <%= link_to (content_for(:title) || "CS15 Bookstore"), root_path, method: :get, class: "text-gray-100 hover:text-white" %>
          </h1>
            <nav class="flex space-x-6 text-sm">
              <% if user_signed_in? %>
                <%= link_to "Home", root_path, class: "text-gray-100 hover:text-white cursor-pointer" %>
                <%= link_to "Browse", books_index_path, class: "text-gray-100 hover:text-white cursor-pointer" %>
                <%= link_to "My Rentals", "#", class: "text-gray-100 text-sm hover:text-white cursor-pointer" %>
                <%= link_to settings_path, class: "text-gray-100 hover:text-white cursor-pointer flex items-center" do %>
                  <svg class="w-4 h-4 mr-1" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M11.49 3.17c-.38-1.56-2.6-1.56-2.98 0a1.532 1.532 0 01-2.286.948c-1.372-.836-2.942.734-2.106 2.106.54.886.061 2.042-.947 2.287-1.561.379-1.561 2.6 0 2.978a1.532 1.532 0 01.947 2.287c-.836 1.372.734 2.942 2.106 2.106a1.532 1.532 0 012.287.947c.379 1.561 2.6 1.561 2.978 0a1.533 1.533 0 012.287-.947c1.372.836 2.942-.734 2.106-2.106a1.533 1.533 0 01.947-2.287c1.561-.379 1.561-2.6 0-2.978a1.532 1.532 0 01-.947-2.287c.836-1.372-.734-2.942-2.106-2.106a1.532 1.532 0 01-2.287-.947zM10 13a3 3 0 100-6 3 3 0 000 6z" clip-rule="evenodd"></path>
                  </svg>
                  Settings
                <% end %>
                <%= button_to 'Log out', destroy_user_session_path, method: :delete, class: "text-gray-100 hover:text-white cursor-pointer" %>
              <% else %>
                <%= link_to "Sign In", new_user_session_path, class: "text-gray-100 hover:text-white cursor-pointer" %>
              <% end %>
            </nav>
        </div>
      </header>

      <main class="flex-1 mx-auto px-6 pt-16 pb-8">
        <%= yield %>
      </main>


      <footer class="bg-white border-t bottom-0 left-0 w-full z-40 shadow-sm">
        <div class="max-w-7xl mx-auto px-6 py-4 text-sm text-gray-500 text-center">
          <%= Time.current.year %> CS15.
        </div>
      </footer>
    </div>
  </body>
</html>
